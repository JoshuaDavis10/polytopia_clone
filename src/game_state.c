#include "game_state.h"

int init_game_state(game_state* state) {
	vec2 mousePos = {0,0};
	vec2 mouseScreenCell = {0,0};
	vec2 mouseOffset = {0,0};
	vec2 mouseWorldCell = {0,0};
	return 1;
}
